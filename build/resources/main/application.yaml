spring:
  application:
    name: Shiprate
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: password
    url: jdbc:mysql://localhost:3306/shiprate
    username: jeb
  jpa:
    show-sql: false
  main:
    allow-bean-definition-overriding: true
logging:
  level:
    com:
      shiprate: INFO
    root: INFO
api:
  oauth:
    access:
      token:
        expiry:
          tolerance: 5
    base:
      url: https://wwwcie.ups.com/
    partner:
      client:
        id: <your OAuth clientId>
      secret: <your OAuth secret>
  rate:
    base:
      url: https://wwwcie.ups.com/api/
    scenario:
      tnt:
        rate:
          day:
            from:
              today: 5
    transaction:
      source: testing
    version: v1

